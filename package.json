{
    "name": "task-manager-root",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=20.9.0"
    },
    "scripts": {
        "build": "cd task-manager-next && npm install && npm run build",
        "start": "cd task-manager-next && npm start"
    }
}