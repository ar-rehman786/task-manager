[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['cd task-manager-next && npm install']

[phases.build]
cmds = ['cd task-manager-next && npm run build']

[start]
cmd = 'cd task-manager-next && node server.js'
